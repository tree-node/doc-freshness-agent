# PROGRESS.md — 進捗と引き継ぎ

最終更新: 2026-08-15（UI・経路A出口・状態管理まで完了）

## 現在のフェーズ

**作るものは通った**（検知→根拠→修正案→修正版DL→判断の保存→監査ログ）。残りは提出物（動画・台本・記事）とブラウザでの目視確認。スケジュール上の残り: 8/12 中間報告① → 8/13〜14 各自作業 → **8/14 22:00 チーム内DL（デモ動画が撮れる状態）** → 8/15 15:00 提出。

## 今やっているタスク

- **実装はいったん区切り。残り時間は提出物へ**。動くものは揃っているので、ここから機能を足しても伝わる量は増えない
- 優先順: ①ブラウザで一度通しで確認（ホーム→変更の詳細→指摘詳細→承認→修正版DL）→ ②デモ動画 → ③push と README のリンク差し替え → ④スライド・記事
- 未実装で残しているもの: 画面④設定・⑤監査ログ（APIはあるが画面が無い）、手動チェックの実行API、定期巡回、経路B

<!-- 旧: 次は UI（バックエンドのAPIとDBスキーマから）。パイプラインは精度の数字まで出たので、残っているのは見せる側と、経路Aの出口（修正版ファイルの生成・DL）
- **`.env` にモデル名を書くこと**（まだ空）。決まった値:
  ```
  MODEL_STAGE0=google/gemini-3.5-flash
  MODEL_STAGE2=google/gemini-3.5-flash-lite
  MODEL_STAGE3=anthropic/claude-sonnet-5
  EMBEDDINGS_MODEL=google/gemini-embedding-001
  ```
- **Stage 3 が逐次実行なので時間がかかる**（1イベント2〜3分）。ライブデモでは待ち時間になる。並列化するか、キャッシュを温めた状態から見せるかを決める必要がある -->

## 次にやる（優先度順）

1. **デモに使う改正の最終選択**（候補調査済み・ブロッカー欄参照）— データ作成の前提
2. **【今晩決定】監視対象の読み取り方式**: ローカルフォルダ推奨（Drive/SharePointは「コネクタ差し替えで対応」とスライド言及。取り込みはDocumentSourceインターフェースで抽象化）。クラウドで見せるならサービスアカウント+共有フォルダ方式のみ検討可（OAuthは半日〜1日リスクで不可）
3. **デモ台本の作成（プレゼン担当・今日から可能）**: 実装の優先順位は台本から逆算（台本駆動）。台本に映らない機能は要件表にあっても後回し
4. ~~e-Gov API v2 の実機確認~~ → **完了（2026-08-13。`scripts/verify_egov.py` 全PASS）**。決定ログに法令ID・asof形式を記録済み
5. 実装順序: CLIでパイプライン貫通（取り込み→チャンク→Stage 0-3、入出力JSON）→ 正解セットで初回評価 → **最後にUI**。UIはFastAPI+SQLite+軽フロントで作り込まない（見栄えはAIモック生成に任せる）
6. **Stage 2の並列数制限を実装に含める**: OrcaRouter無料枠（Hacker）はレート上限がTeamより低く、50件一斉並列は429リスク。セマフォで10〜20並列に制限（フェイルオーバーもあるため実害は出ない見込み）
7. 埋め込みモデルの選定（日本語性能重視）。**OrcaRouterはEmbeddings対応が公式明記**（base_url切替のみ）のため、フォールバックキーは不要見込み・保険は任意。ルーティングDSLの無料枠可否は優先度低（設計はモデル明示指定で、modelパラメータはプラン問わず動作）
8. **モデル具体名（Stage 0/2/3）は議論で決めない**: 正解セット完成後に2〜3候補を流して数字で決定（比較結果はQiita記事の1章に）
9. 画面5枚（ソース登録 / イベント一覧 / イベント詳細 / 指摘詳細 / 監査ログ）— デモ台本に映る順で優先度付け
10. Stage 1/2 の通過閾値の初期値決定（正解セット完成後に実測して調整）
11. 当日リスク対策: テザリング予備 / スナップショットでのオフライン動作確認（e-Gov不通でも動くことの確認＝「本番で通用する」の説明素材）/ ライブデモは事前にキャッシュを温めた状態から / ライブが死んだら動画切り替えの事前合意
12. `.env` + `.gitignore` を初手で設定。リポジトリ公開可否の決定と、Qiita記事スクショへのキー写り込み注意を記事担当に共有

## 完了

- 2026-08-15: **判断の保存と監査ログ**。承認・棄却・検討中がSQLiteに保存され、画面を離れても残る。監査ログは「誰が・いつ・どの改正を根拠に・どう判断したか」を根拠法令とセットで追記のみで残す（対応不要の判断も記録）。テスト170件
- 2026-08-15: **修正版ファイルの生成とダウンロード（経路Aの出口）**。`python -m app.cli propose` と `GET /api/events/{law_id}/revised`。元ファイルには触れず別の場所に生成し、置換先を添えて渡す。生成後に読み直して本当に置換されたかを検証する。実データで休暇規程.docx の第28条第2項が改正後の内容に置き換わることを確認
- 2026-08-14: **画面をバックエンドAPIに接続**。イベント一覧・詳細・見守り中のルール・チェック履歴。返すのはパイプラインの出力そのまま。APIに繋がらないときは手元のデータにフォールバックする（デモ中に落ちても画面が白くならない）
- 2026-08-14: **UIモックをReactに移植**（ホーム / 変更の詳細 / 指摘詳細）
- 2026-08-14: **正解セットでの初回評価とモデル決定**。`scripts/evaluate.py` で正解表と結果JSONを突き合わせ、見逃しと過検出を分けて集計。**見逃し 0/9・過検出 2件**（Stage 3 = claude-sonnet-5）。Stage 2 の上限を実測にもとづき 15→25 に調整（切り捨てが起きていた）。Stage 3 のモデルを sonnet-5 と haiku-4.5 で比較して sonnet-5 に決定（決定ログ参照）。テスト118件パス
- 2026-08-13: **デモデータの受け入れと整備、正本2本化**。ダミーデータ担当から受領した6ファイル＋違反一覧を確認し、監視対象（`demo-data/監視対象/`）と答え合わせ（同 `答え合わせ/`）に分離。厚労省の解説文除去・架空社名の統一・出典明記。DESIGN.md が求めるパターン（ひな形/締結済みの2役、適用外の業務委託契約、重複配置、名前と中身の不一致、AIへの指示文入り文書）を追加。労働基準法を2本目の正本に追加し、**正解表の検知対象8件をすべて実データで検出**（第138条削除のdelete型を含む）。データを流す過程で実装側の不具合6件を発見・修正
- 2026-08-13: **CLIでパイプライン貫通**（取り込み→チャンク分割→インデックス→Stage 0→1→2→3→起票）。`python -m app.cli ingest` / `run` の2コマンド。実データ（e-Gov＋OrcaRouter）で動作確認済み: 育介法の第十六条の二の改正に対し、暫定サンプル35チャンクから **Stage 2で8件に絞り、就業規則2ファイルと雇用契約書ひな形を「即時対応」で検出、業務委託契約書は「適用外」で除外**。コスト実測 **$0.0668/イベント**（Stage 2 は35回で $0.0058）。テスト94件パス
- 2026-08-13: **e-Gov API v2 実機確認 全PASS**（`scripts/verify_egov.py`、標準ライブラリのみ・依存なしで再実行可）。法令ID特定 / 改正履歴21件取得 / asofで改正前後の条文取得 / 条・項の構造化取得（JSON・XML両方）を確認。**時間巻き戻し方式の技術リスクは解消**
- 2026-08-13: **e-Gov クライアント + スナップショット差分を実装**（`app/egov/`、CLI `python -m app.cli`）。時間巻き戻しデモの機構が実データで動作: `snapshot 403AC0000000076 --asof 2025-03-01` → `check 403AC0000000076` で実在の2025年改正を差分として検知（変更23・追加4）。テスト22件パス（うちネットワーク非依存の単体テスト15件）、`scripts/verify_egov.py` は実データ回帰チェックとして全PASS
- 2026-08-13: **プロジェクト雛形作成**。`app/`（FastAPI+SQLite+.env読み込み）と `frontend/`（React+Vite+Tailwind v4）を分離、`/api/health` をVite devプロキシ経由で疎通確認、pytest 7件パス
- 2026-08-13: 実装前調査3件完了。①e-Gov API v2: 時点指定(asof)・認証不要・JSON/XMLを確認、改正前後の条文を直接取得可能。②モデル就業規則: 最新は令和7年12月版（育介法2025施行後の版=反映済みと判断、書き換え時に該当条を目視確認）。**厚労省「育児・介護休業等に関する規則の規定例」R7.4/10施行対応版のWord版がDL可能**→育児介護休業規程のベースに最適（出典明示要）。③OrcaRouter: 無料Hacker枠は完全なゲートウェイ、**Embeddings対応を公式明記**、レート上限は低め→Stage 2並列制限を要実装
- 2026-08-12: デモ用改正の候補調査完了（本命/サブ/delete枠の3候補を特定、ブロッカー欄参照）。法令API v2の時点指定機能を発見
- 2026-08-12: Stage 2/3 プロンプト設計・エスカレーション確定 → DESIGN.md 反映済み。設計フェーズ完了

- 2026-08-12: Stage 0（差分の分解とクエリ化）の出力スキーマ確定 → DESIGN.md 反映済み
- 2026-08-12: 設計ドキュメント v2 確定（DESIGN.md / design-doc.html）。機能スコープ、チャンク分割、絞り込みパイプライン、見逃し担保、キャッシュ・起票分離、デモデータ方針まで

## ブロッカー・人間への確認待ち

- **デモ用改正の最終選択（候補調査は完了・下記から選ぶ）**:
  - 本命候補: 労働条件明示ルール改正（労基法施行規則5条、2024年4月施行）— 雇用契約書・労働条件通知書に直結、雇用形態バリエーション方針と整合
  - サブ候補: 育児・介護休業法 2025年改正（2025年4月/10月施行）— 直近施行済み、育児・介護休業規程に直撃、「正本差し替え」の実演になる
  - デモ映え枠: 労基法138条の削除（2023年4月）— 実在するdelete型ケース。2つ目のイベントとして追加登録用
  - 注意: 「40年ぶり労基法大改正」は法案未提出で条文が存在せず正本に使えない。ただしプレゼンの課題実在性・ビジネス成立性の材料として使う
- ダミーデータ担当への「PDF禁止（md/txt/docx のみ）」の伝達 — **伝達済みかどうか要確認**

## 決定ログ（新しいものを上に追記）

- 2026-08-15: **ロゴは案6「断層」に決定**（10案から選定）。条文の行が途中から横にズレている形で、「改正とは足元が動くこと」を表す。中央の朱がズレて古くなった一行。実装は `frontend/src/components/Logo.jsx`（色は `var(--shu)` 追従）、資料用の単体SVGは `docs/logo/`。**28px 未満では行数を3本→2本に減らしてズレを大きくする**（3本のまま縮めると断層が1px程度になり、ただの横棒に見えるため）
- 2026-08-15: **判断はファイル単位で持つ**（`law_id + change_id + chunk_id + doc_id`）。同じ内容のチャンクが複数ファイルにあっても判断は別々に持てる。判定はチャンク単位・起票はファイル単位、という分離を壊さないため
- 2026-08-15: **監査ログは追記のみ**。判断を変えても過去の判断を消さない。「なぜそう決めたか」を辿れることがプロダクトの主張の一部なので、履歴を消せる作りにしない
- 2026-08-15: **修正版ファイルは生成後に読み直して検証する**。docx はランの分かれ方によって置換が空振りすることがあり、「生成できたつもりで中身が変わっていない」が最も危険な事故のため。1件も当たらなければAPIは422を返し、成功したふりをして元のままのファイルを返さない
- 2026-08-15: **APIが落ちていたらフロントは手元のデータで表示を続ける**。デモ動画の撮影中にバックエンドが落ちても画面が白くならないようにするための保険
- 2026-08-15: **APIは画面用に加工せず、パイプラインの出力をそのまま返す**。サーバー側で整形するとCLIの出力と画面の表示が二重管理になるため、整形はフロントに寄せる
- 2026-08-15: `WATCH_ROOT` の既定値を `./demo-data` から `./demo-data/監視対象` に変更。デモデータを組み直したときに監視対象を1階層下げたのに設定を追随させておらず、修正版生成が元ファイルを見つけられなくなっていた


- 2026-08-14: **Stage 3 のモデルは `anthropic/claude-sonnet-5` に決定**（正解セットでの実測。比較はQiita記事の材料）。デモデータ12文書171チャンク、4つの改正（育介法第16条の2 / 労基法第36条・第39条・附則第138条削除）で測定

  | | claude-sonnet-5 | claude-haiku-4.5 |
  |---|---|---|
  | 見逃し | **0 / 9** | **0 / 9** |
  | 影響なし期待5件の正しい除外 | **4件**（誤指摘1） | 1件（誤指摘4） |
  | 正解表に無い箇所への指摘 | **1件** | 9件 |
  | 4イベント合計コスト | $0.4687 | $0.3592 |

  - **見逃しはどちらもゼロ**。差が出るのは「影響なしを正しく除外できるか」という一番難しいところ
  - Haiku は過検出が6.5倍（2件 → 13件）に対し、コストは23%しか安くない。割に合わない
  - 質の差も出た: Sonnet の過検出は確信度0.55で**要確認フラグ付き**（棄却フローで優先度を付けられる）。Haiku は確信度0.95でフラグ無し——**自信を持って間違える**ので人間が拾いにくい
  - Stage 0 = `google/gemini-3.5-flash` / Stage 2 = `google/gemini-3.5-flash-lite` / 埋め込み = `google/gemini-embedding-001` は据え置き
- 2026-08-14: **DeepSeek系は候補から外す**。`deepseek/deepseek-v4-pro` は単価が sonnet の1/5だが、1判定あたりの生成が長く（平均1,300〜1,400トークン）、4イベントの測定が1時間かかっても終わらなかった。デモの実演に耐えない
- 2026-08-14: **Stage 2 の上限を 15 → 25**。労基法第36条の回で閾値通過が17〜21件あり、15でも20でも切り捨てが起きていた（見逃しには至らなかったが、黙って捨てるのは原則違反）。この上限はコストの歯止めであって絞り込みの手段ではない
- 2026-08-14: **Stage 2 の閾値は 0.5 のまま据え置き**。実測では正解の最低スコアが0.8で、0.8まで上げても見逃しゼロ・過検出が1件減る。それでも上げないのは、0.5が「判断に迷う」ときにモデルが付ける値だから——そこを切ると迷いを落とすことになり、見逃し側に倒す設計と矛盾する。正解セットが9件と小さいことも理由
- 2026-08-14: **精度の報告は行単位で数える**（`scripts/evaluate.py`）。1つの箇所に複数の指摘が付くので、指摘の件数を分母にすると分母が過検出の数で動いてしまう。「影響なし5件中4件を正しく除外」＋「正解表に無い箇所への指摘1件」の形で分けて出す

- 2026-08-13: **監視する正本は2本**。育児・介護休業法 `403AC0000000076`（`asof 2025-03-01`）と 労働基準法 `322AC0000000049`（`asof 2019-03-01`）。労基法を1本入れると第36条・第39条の改正と附則第138条の削除が同じ差分に入り、就業規則・休暇規程・賃金規程の不適合が拾える。**2024年4月の建設業等への適用拡大は労基法本体に条文差分が出ない**（リビジョンが変わらない）ので検知の根拠にできない
- 2026-08-13: **デモデータは「法令違反の一覧」ではなく「改正に紐づく不適合」で作る**。本プロダクトが検知するのは正本の変更で古くなった箇所であり、昔からある法令への違反は変更イベントが無いので検知されない。割増賃金の算定基礎（労基則21条）はこれに該当し、正解表で対象外と明記した——プレゼンではスコープの説明に使える
- 2026-08-13: **正解表・解説は監視対象フォルダの外に置く**（`demo-data/答え合わせ/`）。中に置くと取り込まれ、内容が改正の解説そのものなので必ず「影響あり」と判定される
- 2026-08-13: **判定キャッシュのキーに文書の同一性（中身のハッシュ）を足す**。「雇用契約書のひな形」と「締結済みの雇用契約書」は休暇条項が1文字も違わないため、変更×チャンク×prompt_version だけをキーにすると先に判定した方が使い回され、deadline_type（immediate / on_renewal）を必ず取り違える。ファイル名ではなく中身から作るので、同一内容ファイルの重複配置では従来どおり1回だけ判定される
- 2026-08-13: **Stage 3 のプロンプトにファイル名を渡さない**（ファイル名・フォルダ構造は判定に使わない、に違反していた）。代わりに文書の冒頭400字を渡す。ひな形か締結済み契約かはそこで判断できる
- 2026-08-13: **Stage 0 には差分を機械抽出して明示する**。条文全文を切り詰めて渡すだけだと、変更が末尾にある長い条で変更箇所が切り落とされ、モデルには「ほぼ同じ2つの文章」しか見えない（労基法第39条2,500字で実際に「変更はありません」と要約され見逃した）。あわせて Stage 0 の出力上限を4000に（2000だと長い条でJSONが途中で切れて毎回フォールバックしていた）
- 2026-08-13: **BM25のIDFは常に正になる式（Lucene方式）を使う**。`rank_bm25` の既定IDFは語がコーパスの半数超の文書に出ると負になり、就業規則ばかりのフォルダでは「看護」「労働者」がこれに当たって**順位が反転**していた（一致しない文書が上位）。スコアによる足切りもやめ、落とすのはクエリ語が1つも出てこないチャンクだけにした

- 2026-08-13: **暫定モデル（正解セットで差し替える前提。.env は空のまま）**。動作確認に使った組み合わせ: Stage 0 = `google/gemini-3.5-flash` / Stage 2 = `google/gemini-3.5-flash-lite` / Stage 3 = `anthropic/claude-sonnet-5` / 埋め込み = `google/gemini-embedding-001`。CLIの `--model-stage0` 等で上書きできるので、比較実験は .env を触らず流せる
- 2026-08-13: **コスト実測は OrcaRouter の `/v1/models` の単価表から算出する**（`usage` にコストは入ってこない）。注意: 応答の `model` はフェイルオーバーで `jp.anthropic.claude-sonnet-5` のように変わるため、**単価の引き当てには「こちらが指定したモデル名」を使う**（`Usage.requested_model`）。単価が取れない場合はトークン数だけ表示して処理は続ける
- 2026-08-13: **紐付け済み文書の無条件通過は文字列ではなくフラグで持つ**（`Candidate.linked`）。表示用の通過理由（`reason`）で判定していたため、紐付け済み文書が検索上位にも入ると理由が「検索上位」になり、Stage 2 の無条件通過が効かずスコア次第で落ちていた（見逃し担保①が破れる）。design-guard の指摘で発見・修正済み。回帰テストあり
- 2026-08-13: **判定と起票の二重化を防ぐ**。同一内容チャンクが複数ファイルにあると、各ファイルで判定され（LLM呼び出しはキャッシュで1回）、さらに起票が全ファイルに展開されて二重になる。`expand_alerts` で (変更ID, チャンクID) の重複を除く
- 2026-08-13: 検索インデックスは JSON（チャンク）＋ `.npy`（埋め込み）で保存する。SQLite はUI/API実装時に入れる（CLI段階の入出力はJSONファイルという方針に合わせた）
- 2026-08-13: チャンクのトークン数は文字数から近似する（`CHARS_PER_TOKEN = 1.2`）。段階ごとにモデルが違いトークナイザが一意に決まらないため、`app/chunking.py` の定数1箇所に閉じる
- 2026-08-13: docx は取り込み時に見出しスタイルを md 記法へ変換し、**md と分割経路を共通化**する。python-docx の `document.paragraphs` / `document.tables` は本文順を保たず「表がどの見出しの下にあるか」が失われるため、XMLの並びをたどる

- 2026-08-13: 法令本文の取得形式は **XML ではなく JSON（`json_format=light`）を採用** → DESIGN.md 分割ルールを更新済み。理由: 同じ条・項構造が取れて扱いが軽い。ただし公式に「試行版・仕様変更あり」の注記があるため、パース層を `app/egov/parser.py` に隔離してXMLへ退避可能な状態を保つ
- 2026-08-13: **条文の識別キーは「構造パス」にする（`app/egov/parser.py`）**。実装の落とし穴を実データで踏んだ結果の設計
  - **条見出し（第一条など）だけをキーにしてはいけない**: 育介法は本則79条＋附則100条で、附則の「第一条」だけで37件ある。dictのキーにすると179条が89件に潰れる（初版の検証スクリプトが実際にこれで過少カウントした）
  - キー形式: `本則/第四章/第十六条の二`、`附則(令和六年五月三一日法律第四二号)/第二条`。附則は**改正法令番号（AmendLawNum）で識別**する（位置は改正で動くため）
  - **キーに章名を含めない**: 2025年改正で「第四章 子の看護休暇」→「第四章 子の看護等休暇」のように章名は変わる。含めるとその章の全条が「削除＋追加」に化ける。キーは章番号のみ、章名の変化は `#heading` ユニットとして別途検出する
  - **条（Article）だけを拾うと落ちるものがある**（実データで確認）: ①附則4件は Article を持たず Paragraph が直下 ②施行規則には制定文（EnactStatement）③Chapter配下に Section ネスト。すべて Provision として拾う（原則4「見逃し側に倒す」）
  - 効果: 育介法の抽出ユニット数は 89 → 198。**2025改正の経過措置（附則第二条・第四条の追加）が拾えるようになった**（従来は完全に欠落。DESIGN.md「本則＋対応する経過措置は1変更にまとめてよい」の前提が満たせる）
- 2026-08-13: モジュール構成を確定。`app/egov/client.py`（API呼び出し・リトライは5xxとネットワーク断のみ、4xxはリトライしない）/ `parser.py`（**JSON形式の仕様変更に備えて隔離**。壊れたらここだけXMLへ差し替え）/ `snapshot.py`（スナップショット保存＋差分＝Stage 0 の入力）。スナップショットは `SNAPSHOTS_DIR`（既定 `./data/snapshots`）に法令IDごとのJSON。CLIは `python -m app.cli {revisions|snapshot|check}`
- 2026-08-13: 巡回で差分なしの場合も結果として返す（`check_law` は None を返し、CLIは「変更なし」と表示）。DESIGN.md 原則3・ホームの「最近のチェック履歴」に「変更なし」を出すため、握りつぶさない
- 2026-08-13: **e-Gov 法令API v2 の実機確認済みパラメータ（実装はこれに従う）**
  - 法令ID: 育児・介護休業法 = `403AC0000000076`（平成三年法律第七十六号）/ 同施行規則 = `403M50002000025`（平成三年労働省令第二十五号）。**同名の「船員に関する〜施行規則」`403M50000800036` が別に存在するため、`law_title` 部分一致だけでIDを確定させない**
  - 時点指定: `asof=YYYY-MM-DD`（ハイフン区切り）。**公布日ではなく施行日基準**で「指定時点以前で最新のリビジョン」が返る。`/laws`・`/law_data`・`/keyword` で使用可、`/law_revisions` にはない。`law_revision_id` を直接指定した場合は asof は無視される
  - 本文取得: `GET /api/2/law_data/{law_id}?response_format=json&law_full_text_format=json&json_format=light`。`json_format=light` は `{"Article": {"ArticleTitle", "ArticleCaption", "Paragraph":[{"Num","ParagraphSentence":{"Sentence":[...]}}]}}` の構造で、**条・項を正規表現なしで取り出せる**（XMLでも `Article`/`Paragraph@Num` で同等）。ただし公式に「JSON形式は試行版・仕様変更の可能性あり」の注記あり → **パースは1モジュールに隔離してXMLへ退避可能にしておく**
  - 条単位取得: `elm=Article_16_2`。**枝番はアンダースコア連結**（`Article_16の2` / `Article_16-2` は 400）
  - 改正履歴: `GET /api/2/law_revisions/{law_id}` → `revisions[]` に `amendment_enforcement_date`（施行日）・`current_revision_status`（`CurrentEnforced`/`PreviousEnforced`/`UnEnforced`）。育介法は21件、**2026-10-01施行の未施行改正も既に登録済み**（施行前の先回り検知に使える）
  - 実測差分（`asof=2025-03-01` vs 現行）: 育介法 = 変更23・追加4 / 施行規則 = 変更34・追加48（いずれも削除0）。第十六条の二の見出しが「子の看護休暇」→「子の看護等休暇」。**育介法2025をデモに使う前提は技術的に成立**
    - ※この行の件数は 2026-08-13 に訂正済み。初版の検証スクリプトが条見出しだけをキーにしていたため附則が潰れて過少カウントしていた（旧記載: 育介法 追加2・変更21 / 施行規則 追加44・変更30）
- 2026-08-13: 雛形の構成を確定。`app/config.py`（.env読み込み・相対パスはリポジトリルート基準に解決・キー未設定でもimportは通し `require()` で明示的に落とす）/ `app/db.py`（sqlite3、WAL・foreign_keys ON。**スキーマはパイプライン実装と同時に入れる**）/ `app/main.py`（`/api/health` はキーの値を返さず設定有無のboolのみ）。フロントは Tailwind v4（`@tailwindcss/vite` プラグイン、設定ファイルなし）、Vite dev の `/api` → `localhost:8000` プロキシ。テストは pytest（`tests/conftest.py` で DB_PATH を一時ディレクトリへ向け、リポジトリ内にDBを作らない）
- 2026-08-13: 開発環境はdevcontainer（Anthropicリファレンス実装ベース）+ --dangerously-skip-permissions。ネットワークは許可リスト方式（laws.e-gov.go.jp / api.orcarouter.ai / pypi / npm を追加）。マウントはリポジトリのみ、git pushはリポジトリ限定PAT、.envは予算上限付き開発キーのみ。「機能1つ動いたらcommit+push」を運用に追加。セットアップは45分タイムボックス、詰まったらホスト+acceptEditsに退避。ホストでのdangerously直接実行は禁止

- 2026-08-13: データ方針転換: 本丸=就業規則・社内規定・プライバシーポリシー（厚労省モデル就業規則をベースに改正前へ書き換え・出典明示）。雇用契約書は「ひな形=即時対応」「締結済み=更新時対応」の2役に。Stage 3出力に deadline_type（immediate/on_renewal/none）を追加、UIに「更新時対応」バッジ追加。法的根拠: 締結済み契約は強行法規の直律的効力で書き換え義務なし、就業規則は法令違反で効力なし＋届出義務。部分除外機能・直近1ヶ月フィルタは不採用
- 2026-08-13: 改正候補の優先見直し: 就業規則本丸化に伴い、育介法2025が本命昇格の可能性（労働条件明示はひな形ユースケースで併用可）。最終決定はチーム

- 2026-08-13: 二経路アーキテクチャ採用。経路A（法令→社内文書、修正版DL）=P0、経路B（GitHub実装→設計書、PR作成）=P1（A貫通後のみ）。原則2を「本体を直接書き換えない（提案物を生成し人間が承認）」に改訂——PR作成は原則違反ではなくGit世界の承認フロー
- 2026-08-13: リポジトリは公開提出。初回コミット前に.gitignore、e-Gov出典明示、ダミーデータは架空名限定
- 2026-08-13: 読み取りはDocumentSource抽象化。LocalFolderSource→余裕があればGoogleDriveSource（サービスアカウント方式のみ）/GitHubSource
- 2026-08-13: UI詳細確定（DESIGN.md画面構成参照）。実装順=イベント一覧→イベント詳細→指摘詳細→登録→監査ログ。スタックはFastAPI+SQLite+React(Vite)+Tailwind

- 2026-08-12: Stage 2は1チャンク1コール並列・迷いは0.5以上・出力はscoreのみ。Stage 3は共通プレフィックス先頭のキャッシュ順序固定、出力フィールド順で適用可能性→影響の思考順を強制、逐語引用の機械照合、修正案は文単位、few-shot1例（適用外ケース）、インジェクション対策タグ
- 2026-08-12: エスカレーション: confidence<0.6で強モデル再精査1回→なお低ければ要人間確認フラグ（ステータスとは別軸）。全失敗系は黙って落とさず起票
- 2026-08-12: **設計フェーズ完了**。以降の設計変更は決定ログ+DESIGN.md該当箇所の更新で管理

- 2026-08-12: スコア統合はRRF(k=60)。Stage 1は順位カットのみ（上位50＋各検索単独上位10の救済枠）、閾値はStage 2に集約（0.5通過・上限15、正解セットで調整）
- 2026-08-12: ベクトルDB不使用（numpy+SQLite+.npy）。埋め込みはAPI経由。BM25は文字バイグラム方式（トークナイザは1関数に隔離、Janome差し替え可能に。MeCab系不使用）

- 2026-08-12: イベント→変更（change）の2階層構造を採用。各変更が独立にStage 1〜3を通る。キャッシュキーは変更単位粒度。デモは精度不足なら単一変更の改正で実施（設計は複数対応を維持）
- 2026-08-12: semantic_query（ベクトル用・旧ルール準拠文書に現れそうな表現）と exact_terms（BM25用）を分離。delete型はBM25主軸に分岐

- 2026-08-12: 判定と起票を分離。判定はチャンクハッシュ単位キャッシュ、起票は全ファイルインスタンスに展開
- 2026-08-12: 文書種別は自動推定（安価モデル・確信度付き）。フィルタでなく優先度付けに使用
- 2026-08-12: 監視対象の登録単位はフォルダ（再帰取り込み＋巡回時の新規ファイル検出）
- 2026-08-12: デモは時間巻き戻し方式（スナップショットを改正前条文で初期化し、実在のe-Govとの差分で検知）
- 2026-08-12: データの深さ配分 = 労働系:全部盛り / 個人情報系:最小 / 金融系:スライド言及のみ
- 2026-08-12: `orcarouter/auto` 不使用。段階ごとにモデル明示指定（DSL可否は確認中）
- 2026-08-12: PR自動作成・一括置換・通知・ダッシュボード・SSOは実装しない
- 2026-08-12: 対応形式は md/txt/docx のみ
- （以降、モデル名・ライブラリ選定・閾値などの決定をここに1行ずつ追記）

## セッション引き継ぎメモ

（各セッション終了時にここを書き換える。書く内容: どこまで動くか / 中断箇所と再開手順 / 直近で嵌った点と回避策。前セッションの内容は不要になったら消してよい——履歴は決定ログと完了に残す）

- **どこまで動くか**: 検知から修正版の受け取り、判断の保存、監査ログまで通しで動く。パイプラインは端から端まで動く。`pytest` 94件パス（LLM・ネットワークはすべて差し替え済みでオフライン実行可）、`scripts/verify_egov.py` 全PASS。**DBスキーマ・UI・修正版ファイルの生成（DL）・評価スクリプトはまだ無い**
- **再現手順**（この順で実行すれば実データで動く）:
  ```bash
  export PYTHONPATH=.
  python -m app.cli snapshot 403AC0000000076 --asof 2025-03-01   # 育介法（改正前で初期化）
  python -m app.cli snapshot 322AC0000000049 --asof 2019-03-01   # 労基法（同上）
  python -m app.cli ingest --dir demo-data/監視対象 --out data/index
  python -m app.cli run 403AC0000000076 --index data/index --change-filter 第十六条の二 --out r1.json
  python -m app.cli run 322AC0000000049 --index data/index --change-filter 第百三十八条 --out r2.json
  ```
  モデル名は `.env` が空なので、環境変数か `--model-stage0` 等で渡す（暫定値は決定ログ参照）。`--no-embed` を付ければBM25だけで動く（埋め込みAPIが不調なときの保険）
  **労基法は差分が41件あるので `--change-filter` で絞ること**（全件流すとStage 2だけで2,000回呼ぶ）。デモで見せるのは 第十六条の二 / 第三十六条 / 第三十九条 / 第百三十八条 の4つ
- **デモの見せ方（データ担当の案）**: 労基法を正本から外すと賃金規程・就業規則の指摘が消え、戻すと復活する。正本と社内文書の依存関係をその場で示せる
- **未解決（次に触る人向け）**:
  - **施行日が変更単位ごとに正確でない**: スナップショット(2025-03-01時点)と現行(2025-10-01施行)を比較すると、間にある2025-04-01施行の改正も混ざる。現在は全変更にイベントの施行日（2025-10-01）が付く。緊急度ソートに効くので、正確さが要るならリビジョンを1つずつ辿って改正ごとにイベントを作る必要がある
  - ~~章見出しだけのチャンクが精査に流れる~~ → 修正済み（配下があれば見出しだけのブロックは作らない）
  - **実行ごとに判定が揺れる**（要対応3〜5件）。temperature=0でもゲートウェイのフェイルオーバーでモデル実体が変わるため。評価時は同一条件で複数回流して確認すること
  - **偽陽性が数件出る**。改正対応済みのロジスティクス社の規程や、年休の詳細を書いていない雇用契約書が「影響あり」になることがある（確信度0.55〜0.75）。引用照合に失敗したものは要確認フラグが立つので棄却フローで吸収できるが、正解セットの過検出の数字には乗る
  - **前後文脈からの引用**: Stage 3 が隣のチャンクの文を根拠に引いてくることがある（第24条の判定に第23条の文を引く等）。引用照合が捕まえて要確認フラグが立つので判定ごとは捨てていない
- **嵌った点と回避策（開発環境固有・重要）**: リポジトリが Windows マウント上にあり **シンボリックリンクを作れない**。そのため ①`python -m venv .venv` も `uv venv .venv` もリポジトリ内では失敗する → venv はサンドボックス側 `/home/agent/.venvs/dfa` に作る（`$DFA_VENV`）②`npm install` は `--no-bin-links` が必要。その結果 `node_modules/.bin` が無く **`npm run dev` は使えない** → `node node_modules/vite/bin/vite.js` を直接叩く。**package.json のスクリプトは標準のまま**にしてある（ホスト・他メンバーの環境では `npm run dev` が普通に動くため）
- 予定どおり `.env` は未コミット（`.gitignore:4`）。`MODEL_STAGE0/2/3` は空のまま＝モデル名は正解セット完成後に決定する方針を維持