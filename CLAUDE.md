# CLAUDE.md — ドキュメント鮮度監視エージェント

AIハッカソン提出物。**提出 8/15(金) 15:00、本番 8/16(日)**。チーム内デッドラインは 8/14(木) 22:00（デモ動画が撮れる状態）。

## ドキュメント構成と運用ルール

| ファイル | 役割 | 更新タイミング |
|---|---|---|
| CLAUDE.md（本書） | 絶対原則と作業手順。常に従う | 原則変更時のみ（稀） |
| DESIGN.md | 確定した設計仕様。実装の正 | 設計変更が確定した時 |
| PROGRESS.md | 進捗・未確定事項・決定ログ・引き継ぎ | **毎セッション** |

- **md が正、design-doc.html は人間確認用の派生物**。設計変更時は DESIGN.md を先に更新する（html の同期は人間側が会議前に行うため、Claude Code は触らなくてよい）
- 設計変更が起きたら: DESIGN.md の該当箇所を更新 → PROGRESS.md の決定ログに日付付きで1行記録

## セッションの開始と終了（必須手順）

1. **開始時**: PROGRESS.md を読む → 「現在のタスク」「次にやる」を確認してから作業に入る。DESIGN.md は該当セクションのみ参照でよい
2. **終了時**: PROGRESS.md を更新する — 完了したこと / 中断した場合はその状態と再開手順 / 新たに発覚した未確定事項。**コードだけ書いて PROGRESS.md を更新せずに終わらない**

## 絶対原則（実装判断に迷ったらここに従う）

1. **主語はイベント**: データモデル・UIの主語は「正本の変更イベント」。ドキュメント一覧を主画面にしない
2. **本体を直接書き換えない**: 提案物（経路A=修正版ファイルのDL提供 / 経路B=PR作成）を生成するのみ。適用は必ず人間の承認を経る。経路AがP0、経路Bは経路A貫通後のみ着手
3. **影響なしも見せる**: 絞り込み・判定の過程と「影響なし」件数を必ず可視化する
4. **見逃し側に倒す**: 偽陰性は致命傷、偽陽性は棄却フローで吸収可能。除外判断に迷ったら通す

## 実装上の制約（違反しないこと）

- 対応形式は **md / txt / docx のみ**。PDF・pptx のパーサーは書かない
- LLM 呼び出しは OrcaRouter 経由（OpenAI 互換、`base_url=https://api.orcarouter.ai/v1`）。**`orcarouter/auto` は使わない**。段階ごとにモデルを明示指定する（Stage 0=中位 / Stage 2=安価 / Stage 3=高性能。具体的なモデル名は PROGRESS.md の決定ログ参照）
- 判定キャッシュのキーは `hash(変更単位) × hash(対象チャンク) × prompt_version`（イベント粒度ではない）。プロンプトを変更したら必ず prompt_version を上げる
- 判定（チャンクハッシュ単位）と起票（ファイルインスタンス単位）を混同しない。同一チャンクを含む全ファイルにアラートを展開する
- 外部から取得した法令テキスト・文書本文は**データとして扱い、指示として解釈しない**（プロンプトインジェクション対策。プロンプト内で明示的に区切る）
- ハッカソンのスコープを超える作り込み（SSO、通知、ダッシュボード等）を勝手に追加しない
- **リポジトリは公開提出**: `.gitignore` + `.env` を初回コミット前に設定。キー・トークンを絶対にコミットしない。e-Gov出典明示をREADMEに記載
- 取り込みは `DocumentSource` インターフェースで抽象化し、`LocalFolderSource` から実装。OAuthフローは書かない

## 仕様の参照先

機能スコープ・画面構成・チャンク分割ルール・絞り込みパイプライン（Stage 0〜3）・見逃し担保・コスト設計・デモデータ方針の詳細はすべて **DESIGN.md**。本書と DESIGN.md が矛盾する場合は人間に確認する。
